<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{alertType}} Alert - {{symbol}} | {{appName}}</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            line-height: 1.6;
            color: #333;
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
            background-color: #f4f4f4;
        }
        .container {
            background-color: #ffffff;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 0 20px rgba(0,0,0,0.1);
        }
        .header {
            text-align: center;
            margin-bottom: 30px;
            padding-bottom: 20px;
            border-bottom: 2px solid #007bff;
        }
        .logo {
            font-size: 28px;
            font-weight: bold;
            color: #007bff;
            margin-bottom: 10px;
        }
        .alert-type {
            font-size: 24px;
            font-weight: bold;
            margin-bottom: 10px;
        }
        .alert-type.BUY {
            color: #28a745;
        }
        .alert-type.SELL {
            color: #dc3545;
        }
        .alert-type.HOLD {
            color: #ffc107;
        }
        .symbol-info {
            background: linear-gradient(135deg, #007bff, #0056b3);
            color: white;
            padding: 25px;
            border-radius: 8px;
            margin: 20px 0;
            text-align: center;
        }
        .symbol-name {
            font-size: 32px;
            font-weight: bold;
            margin-bottom: 10px;
        }
        .current-price {
            font-size: 24px;
            margin-bottom: 5px;
        }
        .price-change {
            font-size: 16px;
            font-weight: bold;
        }
        .price-change.positive {
            color: #90EE90;
        }
        .price-change.negative {
            color: #FFB6C1;
        }
        .alert-details {
            background-color: #f8f9fa;
            padding: 25px;
            border-radius: 8px;
            margin: 20px 0;
            border-left: 4px solid #007bff;
        }
        .detail-row {
            display: flex;
            justify-content: space-between;
            margin: 12px 0;
            padding: 8px 0;
            border-bottom: 1px solid #e9ecef;
        }
        .detail-row:last-child {
            border-bottom: none;
        }
        .detail-label {
            font-weight: bold;
            color: #555;
            flex: 1;
        }
        .detail-value {
            color: #333;
            flex: 1;
            text-align: right;
        }
        .recommendation {
            padding: 20px;
            border-radius: 8px;
            margin: 20px 0;
            text-align: center;
            font-weight: bold;
            font-size: 18px;
        }
        .recommendation.BUY {
            background-color: #d4edda;
            border: 1px solid #c3e6cb;
            color: #155724;
        }
        .recommendation.SELL {
            background-color: #f8d7da;
            border: 1px solid #f5c6cb;
            color: #721c24;
        }
        .recommendation.HOLD {
            background-color: #fff3cd;
            border: 1px solid #ffeaa7;
            color: #856404;
        }
        .targets {
            background-color: #e8f4fd;
            padding: 20px;
            border-radius: 8px;
            margin: 20px 0;
            border-left: 4px solid #007bff;
        }
        .target-item {
            display: flex;
            justify-content: space-between;
            margin: 8px 0;
            padding: 5px 0;
        }
        .target-label {
            font-weight: bold;
        }
        .target-value {
            font-family: monospace;
            background-color: #f8f9fa;
            padding: 2px 8px;
            border-radius: 3px;
        }
        .risk-warning {
            background-color: #f8d7da;
            border: 1px solid #f5c6cb;
            color: #721c24;
            padding: 15px;
            border-radius: 5px;
            margin: 20px 0;
            text-align: center;
        }
        .market-context {
            background-color: #f8f9fa;
            padding: 20px;
            border-radius: 8px;
            margin: 20px 0;
        }
        .context-item {
            margin: 8px 0;
            padding: 5px 0;
            border-bottom: 1px solid #e9ecef;
        }
        .context-item:last-child {
            border-bottom: none;
        }
        .timestamp {
            text-align: center;
            color: #666;
            font-size: 14px;
            margin: 20px 0;
            padding: 10px;
            background-color: #f8f9fa;
            border-radius: 5px;
        }
        .footer {
            text-align: center;
            margin-top: 30px;
            padding-top: 20px;
            border-top: 1px solid #eee;
            color: #666;
            font-size: 14px;
        }
        .disclaimer {
            background-color: #fff3cd;
            border: 1px solid #ffeaa7;
            color: #856404;
            padding: 15px;
            border-radius: 5px;
            margin: 20px 0;
            font-size: 12px;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <div class="logo">{{appName}}</div>
            <div class="alert-type {{alertType}}">📈 {{alertType}} ALERT</div>
        </div>
        
        <div class="content">
            <div class="symbol-info">
                <div class="symbol-name">{{symbol}}</div>
                <div class="current-price">₹{{currentPrice}}</div>
                {{#if priceChange}}
                <div class="price-change {{#if (gt priceChange 0)}}positive{{else}}negative{{/if}}">
                    {{#if (gt priceChange 0)}}+{{/if}}{{priceChange}}% 
                    {{#if (gt priceChange 0)}}📈{{else}}📉{{/if}}
                </div>
                {{/if}}
            </div>
            
            <div class="recommendation {{alertType}}">
                {{#if (eq alertType "BUY")}}
                🟢 STRONG BUY SIGNAL
                {{else if (eq alertType "SELL")}}
                🔴 SELL SIGNAL
                {{else if (eq alertType "HOLD")}}
                🟡 HOLD POSITION
                {{/if}}
            </div>
            
            <div class="alert-details">
                <h3>Alert Details</h3>
                <div class="detail-row">
                    <span class="detail-label">Symbol:</span>
                    <span class="detail-value">{{symbol}}</span>
                </div>
                <div class="detail-row">
                    <span class="detail-label">Current Price:</span>
                    <span class="detail-value">₹{{currentPrice}}</span>
                </div>
                {{#if volume}}
                <div class="detail-row">
                    <span class="detail-label">Volume:</span>
                    <span class="detail-value">{{volume}}</span>
                </div>
                {{/if}}
                {{#if marketCap}}
                <div class="detail-row">
                    <span class="detail-label">Market Cap:</span>
                    <span class="detail-value">₹{{marketCap}}</span>
                </div>
                {{/if}}
                <div class="detail-row">
                    <span class="detail-label">Alert Triggered:</span>
                    <span class="detail-value">{{alertTime}}</span>
                </div>
            </div>
            
            {{#if targets}}
            <div class="targets">
                <h3>Price Targets</h3>
                {{#if targets.entry}}
                <div class="target-item">
                    <span class="target-label">Entry Price:</span>
                    <span class="target-value">₹{{targets.entry}}</span>
                </div>
                {{/if}}
                {{#if targets.target1}}
                <div class="target-item">
                    <span class="target-label">Target 1:</span>
                    <span class="target-value">₹{{targets.target1}}</span>
                </div>
                {{/if}}
                {{#if targets.target2}}
                <div class="target-item">
                    <span class="target-label">Target 2:</span>
                    <span class="target-value">₹{{targets.target2}}</span>
                </div>
                {{/if}}
                {{#if targets.stopLoss}}
                <div class="target-item">
                    <span class="target-label">Stop Loss:</span>
                    <span class="target-value">₹{{targets.stopLoss}}</span>
                </div>
                {{/if}}
            </div>
            {{/if}}
            
            {{#if analysis}}
            <div class="market-context">
                <h3>Market Analysis</h3>
                <div class="context-item">
                    <strong>Technical Indicators:</strong> {{analysis.technical}}
                </div>
                {{#if analysis.fundamentals}}
                <div class="context-item">
                    <strong>Fundamentals:</strong> {{analysis.fundamentals}}
                </div>
                {{/if}}
                {{#if analysis.sentiment}}
                <div class="context-item">
                    <strong>Market Sentiment:</strong> {{analysis.sentiment}}
                </div>
                {{/if}}
            </div>
            {{/if}}
            
            <div class="timestamp">
                <strong>Alert Generated:</strong> {{alertTime}}<br>
                <strong>Valid Until:</strong> {{validUntil}}
            </div>
            
            <div class="risk-warning">
                <strong>⚠️ Risk Warning:</strong> Trading involves substantial risk. Past performance does not guarantee future results. Please trade responsibly and only invest what you can afford to lose.
            </div>
            
            <div class="disclaimer">
                <strong>Disclaimer:</strong> This alert is for informational purposes only and should not be considered as financial advice. Please conduct your own research and consult with a qualified financial advisor before making any investment decisions. {{appName}} is not responsible for any trading losses.
            </div>
        </div>
        
        <div class="footer">
            <p>Received this alert as part of your {{planName}} subscription</p>
            <p>Questions? Contact us at <a href="mailto:{{supportEmail}}">{{supportEmail}}</a></p>
            <p>Alert ID: {{alertId}} | User ID: {{userId}}</p>
            <p>&copy; {{currentYear}} {{appName}}. All rights reserved.</p>
        </div>
    </div>
</body>
</html>