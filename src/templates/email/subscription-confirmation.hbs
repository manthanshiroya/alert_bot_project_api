<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Subscription Activated - {{appName}}</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            line-height: 1.6;
            color: #333;
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
            background-color: #f4f4f4;
        }
        .container {
            background-color: #ffffff;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 0 20px rgba(0,0,0,0.1);
        }
        .header {
            text-align: center;
            margin-bottom: 30px;
            padding-bottom: 20px;
            border-bottom: 2px solid #28a745;
        }
        .logo {
            font-size: 28px;
            font-weight: bold;
            color: #007bff;
            margin-bottom: 10px;
        }
        .success-message {
            font-size: 24px;
            color: #28a745;
            margin-bottom: 20px;
        }
        .subscription-details {
            background-color: #f8f9fa;
            padding: 25px;
            border-radius: 8px;
            margin: 20px 0;
            border-left: 4px solid #28a745;
        }
        .detail-row {
            display: flex;
            justify-content: space-between;
            margin: 10px 0;
            padding: 8px 0;
            border-bottom: 1px solid #eee;
        }
        .detail-row:last-child {
            border-bottom: none;
        }
        .detail-label {
            font-weight: bold;
            color: #555;
        }
        .detail-value {
            color: #333;
        }
        .features {
            background-color: #e8f5e8;
            padding: 20px;
            border-radius: 8px;
            margin: 20px 0;
        }
        .feature-item {
            margin: 8px 0;
            padding-left: 20px;
            position: relative;
        }
        .feature-item:before {
            content: "✓";
            position: absolute;
            left: 0;
            color: #28a745;
            font-weight: bold;
        }
        .amount {
            font-size: 20px;
            font-weight: bold;
            color: #28a745;
        }
        .footer {
            text-align: center;
            margin-top: 30px;
            padding-top: 20px;
            border-top: 1px solid #eee;
            color: #666;
            font-size: 14px;
        }
        .important-note {
            background-color: #fff3cd;
            border: 1px solid #ffeaa7;
            padding: 15px;
            border-radius: 5px;
            margin: 20px 0;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <div class="logo">{{appName}}</div>
            <div class="success-message">🎉 Subscription Activated!</div>
        </div>
        
        <div class="content">
            <p>Congratulations, <strong>{{userName}}</strong>!</p>
            
            <p>Your subscription has been successfully activated. You now have full access to premium trading alerts and features.</p>
            
            <div class="subscription-details">
                <h3>Subscription Details</h3>
                <div class="detail-row">
                    <span class="detail-label">Plan:</span>
                    <span class="detail-value">{{planName}}</span>
                </div>
                <div class="detail-row">
                    <span class="detail-label">Start Date:</span>
                    <span class="detail-value">{{startDate}}</span>
                </div>
                <div class="detail-row">
                    <span class="detail-label">End Date:</span>
                    <span class="detail-value">{{endDate}}</span>
                </div>
                <div class="detail-row">
                    <span class="detail-label">Amount Paid:</span>
                    <span class="detail-value amount">{{currency}} {{amount}}</span>
                </div>
            </div>
            
            <div class="features">
                <h3>Your Plan Includes:</h3>
                {{#if planFeatures.maxAlertConfigs}}
                    {{#if (eq planFeatures.maxAlertConfigs -1)}}
                        <div class="feature-item">Unlimited Alert Configurations</div>
                    {{else}}
                        <div class="feature-item">Up to {{planFeatures.maxAlertConfigs}} Alert Configurations</div>
                    {{/if}}
                {{/if}}
                {{#if planFeatures.maxOpenTrades}}
                    <div class="feature-item">Up to {{planFeatures.maxOpenTrades}} Simultaneous Open Trades</div>
                {{/if}}
                {{#if planFeatures.prioritySupport}}
                    <div class="feature-item">Priority Customer Support</div>
                {{/if}}
                {{#if planFeatures.advancedAnalytics}}
                    <div class="feature-item">Advanced Analytics & Reporting</div>
                {{/if}}
                <div class="feature-item">Real-time Trading Alerts via Telegram</div>
                <div class="feature-item">Detailed Trade History & Performance Tracking</div>
                <div class="feature-item">Risk Management Tools</div>
            </div>
            
            <div class="important-note">
                <strong>Important:</strong> Your alerts will start being delivered immediately. Make sure your Telegram notifications are enabled to receive real-time trading signals.
            </div>
            
            <p>Thank you for choosing {{appName}}. We're committed to helping you achieve your trading goals!</p>
        </div>
        
        <div class="footer">
            <p>Questions? Contact us at <a href="mailto:{{supportEmail}}">{{supportEmail}}</a></p>
            <p>&copy; {{currentYear}} {{appName}}. All rights reserved.</p>
        </div>
    </div>
</body>
</html>